[package]
name = "models"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.39"
rand = "0.9.0"
serde = { version = "1.0.217", features = ["derive"] }
sqlx = "0.8.3"
ulid = { version = "1.2.0", features = ["postgres", "serde"] }
